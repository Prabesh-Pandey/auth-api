## Auth api -- REST APIs

## JWT and bcrypt

## Quick start

- **Description:** Simple JWT-based authentication API using bcrypt for password hashing.
- **Endpoints:** `/register`, `/login`, `/profile` (protected).
- **Run:** `npm install` then `npm start`.
- **Tests:** `npm test`.
  ## test1
